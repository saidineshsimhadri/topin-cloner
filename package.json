{
  "name": "topin_clone",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium && npx playwright install-deps chromium || echo 'Playwright install failed, will try at runtime'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "csv-parse": "^6.2.1",
    "csv-stringify": "^6.7.0",
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "playwright": "^1.58.2"
  }
}
